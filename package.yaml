name:                vallance-tracker
version:             0.1.0.0
github:              "githubuser/vallance-tracker"
license:             BSD3
author:              "Author name here"
maintainer:          "example@example.com"
copyright:           "2020 Author name here"

extra-source-files:
- README.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/githubuser/vallance-tracker#readme>

dependencies:
- base >= 4.7 && < 5
- relude
- aeson
- postgresql-simple
- req
- resource-pool
- servant-js
- servant-server
- time
- wai
- warp

library:
  source-dirs: src
  default-extensions:
    - NoImplicitPrelude
    - OverloadedStrings

executables:
  vallance-tracker-exe:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - base
    - vallance-tracker
    - filepath

tests:
  vallance-tracker-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - base
    - vallance-tracker
    - hspec
    - hspec-wai
    - hspec-wai-json
    - aeson
